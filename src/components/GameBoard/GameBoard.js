import React from 'react';


function Game() {
    // THINGS TO HAVE IN STATE:
    // whose turn it is
    // what the board looks like
    // who is this player? or that in app?

    return (
        <div className="GameBoard">
            <h2>Game Board Component</h2>
        </div>
    );
}

export default Game;